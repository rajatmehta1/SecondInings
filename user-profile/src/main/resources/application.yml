spring:
  datasource:
    url: jdbc:mysql://localhost:3306/si_db?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true # turn to false later
  cloud:
  application:
    name: user-profile
server:
  port: 9010

